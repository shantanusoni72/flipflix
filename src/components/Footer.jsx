import dashboard from '../dashboard'

export default function Footer() {
    return (
        <div className="footer">
            <h6>{ dashboard.landing_page.footer.title }</h6>
        </div>
    )
}
